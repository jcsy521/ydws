# -*- coding: utf-8 -*-

from utils.dotdict import DotDict

CATEGORY = DotDict(PROVINCE=10,
                   CITY=20)

# if you can work on all provinces, set area_id to 0.
ALL_PROVINCES = 0

# province id for every province
PROVINCE = DotDict(LIAONING=240)
